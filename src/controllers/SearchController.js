const { Router } = require('express');
const { DevController } = require('../models/Dev');

module.export = {
    async index(req, res) {
        // buscar todos os devs em um raio de 10km
        // filtrar por tecnologias usadas
        
    }
}